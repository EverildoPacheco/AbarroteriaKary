@{
    ViewData["Title"] = "Acceso denegado";
}
<div class="container py-5">
    <div class="alert alert-warning">
        <h5 class="mb-2">Acceso denegado</h5>
        <p>No tienes permisos para acceder a esta opción. Si crees que es un error, contacta al administrador.</p>
        <a class="btn btn-outline-secondary" href="@Url.Action("Inicio", "Home")">Ir al inicio</a>
    </div>
</div>
